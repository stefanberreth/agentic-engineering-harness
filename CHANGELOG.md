# Changelog

All notable changes to AEH (Agentic Engineering Harness) are documented in this file.

Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/). This project does not yet use semantic versioning -- versions are sequential milestones (v0.1, v0.2, etc.) reflecting capability growth.

Target-project-specific work (prompts, deliverables, assessments, journal entries) is NOT tracked here. That work lives in `targets/<project>/` and is tracked per-project in `tasks.md` and `journal.md`.

---

## [Unreleased]

### Added
- **Reviewer quality: an over-engineering scrutiny dimension, an elite-adversarial stance, and the simplicity angle in the retrospectives** (`templates/personas/reviewer.md` §2 gains an "Over-Engineering & LLM-Typical Waste" dimension -- the mirror of the Absence Check, tuned to LLM-characteristic waste: speculative abstraction, defensive-check sprawl, duplicated logic, reinvented helpers, prolific small files, state passed back and forth, premature generality; findings are non-blocking simplification suggestions by default, blocking only when the over-build creates real maintenance/bug/security surface; a new `over_engineering` verdict category and a mirror Principles bullet make it reportable and durable; `reviewer.md` "What You Are" reframes the reviewer as an elite, adversarial reviewer whose default stance is to assume there IS a flaw and hunt it -- adversarial toward the artifact, never the author, applied with taste; `reviewer.md` Retrospective Evaluation now surfaces and routes the developer's simplicity insights; `templates/personas/developer.md` §7 retrospective gains the explicit "could this have been substantially simpler?" question, a sharpening of the existing guidance rather than a rewrite; `templates/personas/harness-reviewer.md` Dimension 6 registers the new reviewer dimension + adversarial stance so a future harness-review verifies they persist). Completes the reviewer-quality half (items 3-5) of the retrospective-convention capture; the elicitation half (items 1-2) shipped as `retrospective-elicitation-convention`. The reviewer sits at the gate with the fresh adversarial distance the implementer structurally lacks -- exactly the vantage to catch over-engineering the doer is too close to see, and the place where a "two rows, not five hundred lines" call still has traction. Outside-in complement to the doers' inside-out hindsight: architect captures simplicity at the design origin, the doer in hindsight, the reviewer at the gate. Authored under `openspec/changes/reviewer-over-engineering-and-adversarial-stance/`; closes inbox capture `_intake/2026-06-05-1907-*` (all 5 items dispositioned).

- **Base-template layer hygiene: target base templates must not reference harness-only constructs** (`templates/personas/harness-reviewer.md` Dimension 4 "Layered Persona Architecture" gains a no-cross-layer-construct-references check). The harness has two persona layers sharing one directory: TARGET-facing base templates (`architect`/`analyst`/`archaeologist`/`developer`/`reviewer`, which propagate into a target's `docs/AE/personas/_base/`) and HARNESS-side roles (`orchestrator`/`harness-reviewer`). A base template must not reference a construct that exists only in the harness layer (the orchestrator, the harness-reviewer, a harness-reviewer Dimension number, the harness CLAUDE.md tree, the `_intake` inbox, the additive-ratchet/forgetting framing) -- a target architect pointed at "the harness-reviewer's Dimension 3" follows a dead reference. Motivated by an actual leak this session: a base-template edit pointed the target architect at a harness-reviewer-only lens; the operator caught it, no standing check did. This makes it a standing Dimension-4 check. Deliberately a judgment check, not a `validate-personas.sh` grep (the leak is paraphrase-class). Authored under `openspec/changes/base-template-layer-hygiene/`.

- **Retrospective elicitation: architect writes its own design retrospective; orchestrator owns eliciting them** (`templates/personas/architect.md` gains a `§7b` "Design Retrospective" subsection -- after a design is handed off, the target architect writes a simplicity-focused hindsight retrospective to `docs/AE/reports/design-<slug>-retrospective.md`, mirroring the developer's §7 format but aimed at the design SHAPE, because over-engineering originates at design; `templates/personas/orchestrator.md` Principles gains an "Elicit hindsight retrospectives -- do not wait to be asked" responsibility -- the orchestrator builds an explicit simplicity-framed hindsight ask into the report-back of every substantive prompt it dispatches, rather than trusting the target persona's own end-of-task retrospective to fire on tactical/ops/investigation prompts where it often does not). Wires the elicitation half of the existing hindsight-retrospective convention so the most valuable reflection -- "in hindsight this could have been two deleted rows, not five hundred new lines in fifteen places" -- surfaces reliably instead of only when the operator remembers to ask (the motivating instance: the orchestrator-state-consolidation session's closing retrospective had to be explicitly requested). Layer-distinct by design: the architect retrospective is the target architect reflecting on the target's design; the orchestrator elicitation is a harness-side responsibility directed at the target agents it dispatches; the two do not cross-reference each other's constructs. Deliberately scoped to elicitation only -- the same `_intake` capture's reviewer-quality items (a first-class over-engineering / LLM-typical-waste review dimension, and an elite-adversarial reviewer characterisation) are a separate thread and remain open, not bundled here (decouple discipline). Authored under `openspec/changes/retrospective-elicitation-convention/`; inbox provenance `openspec/changes/_intake/2026-06-05-1907-*` (items 1-2 of 5).

- **Subtraction-completeness discipline (sweep producers + consumers before a removal is "done")** (`templates/personas/architect.md` §8 Principles gains a plan-time bullet -- a design that removes/renames/folds a convention enumerates producers + consumers and `tasks.md` carries the sweep as explicit tasks with a residual-scan completion signal; `templates/personas/reviewer.md` Principles gains the inverse-of-the-Absence-Check rule -- a change that retires a token is incomplete until a repo-wide residual scan is clean, a surviving reference outside a labelled migration note is a finding; `templates/personas/harness-reviewer.md` Dimension 4 gains the explicit subtraction-completeness check paired with the Dimension-3 "still earns its place" forgetting question). Addition has always been safe-by-default in this harness; subtraction has had no discipline -- the additive ratchet that motivated `orchestrator-state-consolidation` is, at root, the absence of a safe removal operation (you cannot confidently take a rule or state slot away without finding every place that produces or consumes it, and no standing rule said you must). The motivating instance is concrete: that consolidation's first implementation pass updated the declaration (the persona allowlist + CLAUDE.md tree) but left the machinery behind (onboarding scaffolded the retired files, health-check read them, the tools playbook recorded into them), caught only because a review happened to grep broadly. This makes the producer+consumer sweep a standing discipline in the three roles that author and review subtractions, so the next removal does not repeat it. It is the symmetric partner of the forgetting question added by `orchestrator-state-consolidation`: that lens decides *whether* to remove; this ensures a removal is *complete* -- declaration and machinery together -- rather than forked into a self-contradiction. No new mechanism: three Principles-level additions propagated through the base personas, mirroring the document-placement ground-truth discipline. Deferred companions: the `CLAUDE.md` Working-Rules pointer rides `claude-md-size-discipline` (CLAUDE.md is at its size ceiling), and a continuous declaration/machinery coherence AUDIT is left to `harness-maintainer-role-charter`. Authored under `openspec/changes/subtraction-completeness-discipline/`; inbox provenance in `openspec/changes/_intake/2026-06-06-1255-*` and `2026-06-06-1256-*`.

- **Polish Mode -- third operating regime for tactical iteration alongside spec-first default** (`templates/personas/orchestrator.md` gains a "Polish Mode" section alongside Operating Modes covering activation phrase, scope IN/OUT, two-bucket triage IMMEDIATE-FIX/DEFERRED-TRIAGE, exit ceremony, openspec-record decision tree; `templates/personas/developer.md` gains a "Polish Mode posture" section covering live-dialogue affordances, scope-boundary halting, exit-ceremony obligations; `templates/prompts/polish-mode.md.template` new canonical session-wrapper the orchestrator instantiates per polish session with placeholders for surface name, change-slug, in-scope routes, dev-environment pointer; `templates/governance/review-criteria.md` gains § 4a Polish-pass review -- a lightweight criterion distinct from the full 10-dimension reviewer rubric covering scope-no-creep, no-regression, tokens-only, audit-trail coverage, commit hygiene; `README.md` gains a Polish Mode section documenting it as a first-class regime). Polish Mode is for low-friction tactical iteration on visible surfaces -- copy, layout, tokens, microcopy -- where the spec-first ceremony (analyst -> architect -> developer -> reviewer) is overkill: operator's intent IS the spec, visual outcome IS the acceptance criterion, operator's eyeball IS the reviewer. The developer holds a strict scope boundary: API/schema/data-model/new-tests'-assertions/new-deps/AC-changes/security-boundary/new-tokens are OUT and cause the mode to halt automatically. Every operator observation is categorised as IMMEDIATE-FIX (apply now, report diff, vite HMR loop) or DEFERRED-TRIAGE (capture in a session-local deferred-items.md for later spec-first cycle). Exit ceremony produces a session log + an openspec record (amendment to active CP's design.md revision history OR a new polish change-slug) + a lightweight reviewer self-check before batch commit + push. Closes the friction-vs-discipline dilemma where operators previously either burned four prompts per text tweak or bypassed ceremony informally. Target propagation rides on the existing `refresh-base-personas` mechanism -- no new retrofit prompt required. First exercise of the regime ran on a target session the same day as the proposal landed. Authored under `openspec/changes/polish-mode-operating-regime/` (proposal + design + tasks).

- **Document placement ground-truth scan discipline (RESPECT / CONSOLIDATE / ESTABLISH) across content-producing personas** (`CLAUDE.md` Working Rules gains a new "Ground-truth scan before writing any new document" bullet codifying the three-branch discipline with role-applicability list; `templates/personas/analyst.md`, `templates/personas/architect.md`, `templates/personas/developer.md`, `templates/personas/reviewer.md`, `templates/personas/archaeologist.md`, `templates/personas/orchestrator.md`, `templates/personas/harness-reviewer.md` each gain a one-bullet Principles entry scoped to the persona's content class; reviewer + harness-reviewer bullets include the enforcer clause -- a new doc in a fresh location with no ground-truth scan evidence is a Dimension-1 hygiene finding; `templates/governance/review-criteria.md` § 4 Governance & Process Quality gains a "Document placement discipline" criterion). Before any role authors a new markdown artefact (runbook, spec, report, deliverable, persona overlay, how-to, design doc, review file), the role must scan existing convention and pre-existing material on the same topic, then pick exactly one of: RESPECT (write at the location existing convention dictates and follow its format), CONSOLIDATE (update existing same-topic material in place and convert duplicates into one-line pointers), or ESTABLISH (no convention exists -- pick a defensible location and wire it into mkdocs nav + CLAUDE.md + relevant persona overlay pointers so future role-holders discover it). Closes the scattered-duplicate-docs anti-pattern where files in `docs/`, `docs/AE/`, `openspec/`, `targets/`, project root would be written once and found never. Reviewer role's enforcer clause makes the discipline self-enforcing on target work; harness-reviewer's mirror clause makes it self-enforcing on the public harness repo (the harness practises what it preaches). Target propagation rides on the existing `templates/prompts/refresh-base-personas.md.template` mechanism -- no new retrofit prompt required. Authored under `openspec/changes/document-placement-ground-truth-scan-discipline/` (proposal + tasks).

- **Cross-container isolation: ownership markers, per-host scheduler lock resolver, journal attribution convention, retrofit prompt** (`bin/resolve-target-owner.sh` new helper -- read/write/check/hostname operations on per-target `.owner-container` marker files at `targets/<slug>/.owner-container`; `bin/resolve-scheduler-lock.sh` new helper -- per-hostname scheduler lockfile path resolver mirroring the persona-marker pattern; `targets/.gitignore` extended to exclude `.owner-container` files; `templates/personas/orchestrator.md` § "Before You Start" new step 6 invoking `--check` with operator-gated handling of peer-owner / no-marker outcomes + new H2 section "Cross-Container Caveats" with the full discipline; `templates/playbooks/health-check.md` new check 3o "Cross-Container Ownership Marker" verifying presence, owner match, and recency; `templates/prompts/seed-target-owner.md.template` new one-shot retrofit prompt for existing targets; `CLAUDE.md` Harness Maintenance Discipline gains a new "Cross-container isolation" bullet + Project Structure tree updated to include new bin/ + prompts/ entries). Closes the contamination risks documented in `openspec/changes/harness-cross-container-isolation/`: silent cross-container writes to peer-owned target workspaces, scheduler-lockfile collision under parallel containers, untracked journal attribution. Per-host persona marker (`bin/resolve-persona-marker.sh`) was already in place; this work completes the isolation discipline by adding the per-target ownership layer and the scheduler-lock layer. The `~/.claude/projects/` future-risk note is documented in the "Cross-Container Caveats" section. The `.claude/settings.local.json` cross-container accumulation is documented as a known limitation (Claude Code owns the path; upstream-issue path planned). Unblocks safe parallel operation of multiple AEH orchestrator sessions across containers, each driving its own target.

- **Base-persona refresh: all-personas scope** (`templates/prompts/refresh-base-personas.md.template` new -- copies all six engineering personas from harness master into target's `docs/AE/personas/_base/` with byte-identical checksum verification, single commit, per-persona before/after size report; `templates/personas/harness-reviewer.md` § "Propagation-Impact Assessment Mode" updated so the example retrofit-action for "Refresh persona snapshots" explicitly scopes to all six personas not orchestrator-only, references the new template). The propagation-flow refresh discipline is now `_base/`-directory-scoped: single-persona refresh leaves pre-existing drift on the other five silently in place; full-directory refresh costs the same operator action and catches accumulated drift. Surfaced during the first verified end-to-end exercise of the propagation flow when the harness-improvement session's retrofit plan refreshed only orchestrator and the peer AEH orchestrator's Propagation-Impact Assessment Mode pass caught developer + reviewer had also drifted. Closes the gap captured at commit b43bea9.

- **OpenSpec close-out convention (proposal-closing side scaffolded at setup time + harness-self dogfooding)** (`templates/tools/openspec-setup.md` Step 1 extended to scaffold `openspec/AGENTS.md` with canonical close-out playbook content + `openspec/changes/archive/` directory; `templates/tools/openspec-teardown.md` preserves both archive and AGENTS.md on teardown; `templates/prompts/openspec-close-out-retrofit.md.template` new retrofit prompt for pre-existing targets with already-installed `openspec/` trees; harness-self `openspec/AGENTS.md` and `openspec/changes/archive/README.md` created as dogfooding). Closes the gap where the harness shipped only the proposal-authoring side of OpenSpec; every target adopting OpenSpec via the harness inherited a missing close-out convention and hit a wall the first time a change proposal completed (no AGENTS.md playbook, no changes/archive/ directory, no documented flow for applying deltas to parent specs and bumping their updated date). New targets onboarded after this change get the close-out side wired in alongside the authoring side. Pre-existing targets adopt via the retrofit prompt: it creates AGENTS.md + changes/archive/ if missing, and optionally walks any structurally-closed proposals through the mechanical close-out under the new convention (operator-confirmed per proposal). Canonical close-out sequence: apply spec deltas to parent specs -> bump parent `last-updated-by:` + `updated:` -> set proposal `status: archived` + add `archived-at:` -> `mv openspec/changes/<slug>/ openspec/changes/archive/<slug>/`. Single commit per close-out with documented message format. Edge cases handled (blocked-at-logical-close stays at `ready-for-archive`; abandoned proposals archive with `status: abandoned` + reason; supersession noted in successor proposal; process/mechanism proposals with no formal spec are no-ops on spec-delta steps then archive normally). Closes the "OpenSpec close-out convention missing from harness setup" gap captured at commit fb32633 and triaged at commit 30a0a68.

- **Harness update propagation signal (target-side stale-detection + harness-reviewer interpretation gate)** (new `harness-sync-sha:` field in `profile.md` template via `templates/playbooks/onboarding.md` Phase 3e + brownfield profile.md schema; new step 5 in `templates/personas/orchestrator.md` "Before You Start" for the session-init detection + new H2 section "Harness Update Propagation Signal" with full discipline; new "Propagation-Impact Assessment Mode" subsection in `templates/personas/harness-reviewer.md`; new check 3n "Harness Sync Marker" in `templates/playbooks/health-check.md`; new retrofit prompt template at `templates/prompts/seed-harness-sync-marker.md.template`; `CLAUDE.md` Harness Maintenance Discipline gains a new "Harness update propagation signal" bullet). Each target's `profile.md` carries a `harness-sync-sha:` field recording the harness commit SHA at last sync. On every orchestrator session-init (target-side and harness-side), the persona reads the marker and compares against current harness HEAD via `git -C /workspace/aeh rev-parse HEAD`; if the SHAs differ, surfaces "Harness has advanced N commits since last sync. Say 'review changes' to run a harness-reviewer pass that scopes local impact." Operator says `review changes` -- harness-reviewer loads in Propagation-Impact Assessment Mode and produces a structured retrofit-action list (what / reason / effort / side-effects / recommended order per action; "no action needed" is a valid output). Operator decides per-action: apply / defer / skip. Marker bumps only to the SHA covering all applied + explicitly-skipped commits; dismissing without action does NOT bump (signal re-surfaces next session-init). Mechanism is symmetric counterpart to the harness capture inbox: inbox flows insights upstream, sync-marker flows updates downstream; both filesystem-mediated, both session-init surfaced, both operator-gated, both stop short of automation. New targets seed at onboarding time; pre-existing targets seed via the retrofit prompt template. Health-check verifies marker presence, validity, and reasonable range. Closes the "harness updates have no in-session signal to existing target sessions" gap captured at commit 067947b and triaged at commit 30a0a68.

- **Harness capture inbox (filesystem-mediated cross-session intake)** (`openspec/changes/_intake/` with README; `.gitignore` extended with `openspec/changes/_intake/.tmp.*`; `templates/personas/orchestrator.md` new "Harness Capture (proactive identification, operator-gated)" section + new step 4 in "Before You Start" for inbox scan + step renumbering; `CLAUDE.md` Harness Maintenance Discipline gains a new "Harness capture inbox" bullet + Project Structure tree updated to include `_intake/`). Cross-session harness-level insights now flow through `openspec/changes/_intake/` -- a filesystem-mediated inbox visible to any orchestrator session via the shared bind-mount. Capture-side orchestrators (in any container, including target sessions where the orchestrator role is active) proactively identify candidates and ASK the operator before writing -- never silent capture; operator confirms; file is written atomically (write `.tmp.<name>`, rename) so cross-container readers never observe a half-written file. Filenames carry a `<HOSTNAME>` suffix to prevent collision when two containers capture at the same minute on the same topic. Triage-side (harness orchestrator session) scans for `status: untriaged` on session-init, surfaces the count in the post-banner summary, and walks captures into proper `openspec/changes/<slug>/` proposals on operator request. Two landing points exist deliberately: `openspec/changes/_intake/` (tracked, public, target-detail-free for the public review pipeline) and `BACKLOG.md` (untracked, private, target context permitted for motivational rationale) -- decision is at capture time. External contributors continue to enter through GitLab/GitHub PRs and Issues; the inbox is for cross-session capture from operator-driven work, not a contributor-facing channel. The publication gate scans `_intake/` files like any other staged content; target-detail leakage fails commit. Closes the operator-paste-shuttle friction where harness-level insights surfaced in target sessions had no robust capture path. Authored under the harness-self OpenSpec discipline introduced in the previous CHANGELOG entry; this is the second OpenSpec change proposal to land.

### Changed
- **Orchestrator state consolidation + pre-clear reconciliation gate + harness-reviewer forgetting question (description + full consumer sweep)** (`templates/personas/orchestrator.md` Principles ground-truth-scan bullet reduces the canonical state set from "eleven canonical filenames" to eight canonical entries, gains a "State model and journal tagging" subsection [function-based model + `[SESSION]`/`[DECISION]`/`[REVIEW]`/`[GATE]` tags] and a "Pre-clear reconciliation" subsection [reconstruct-and-diff procedure + `pre-clear delta:` log line + fix-the-slot-not-the-instance rule], adds an `## Open Questions` section to the state-file format template, and updates the State Initialisation + cross-reference lines; `CLAUDE.md` "Target Project Workspace Structure" tree + body lines drop the retired files and note where the folded content now lives; `templates/personas/harness-reviewer.md` Dimension 3 [Documentation Currency] gains the "does every always-active rule and canonical state slot still earn its place" forgetting question, and its lift-candidate extended-scan points at journal-tag greps; `templates/prompts/migrate-state-satellites.md.template` new one-shot retrofit prompt. **Consumer sweep:** every harness file that records into or reads from the retired files was moved to the new model and passed through the simplification / de-duplication gate, not just repointed -- `templates/playbooks/onboarding.md` (workspace scaffold, assessment flow, opt-out recording, re-onboard backup removed, close-out gates), `templates/playbooks/health-check.md` (baseline read, standard-tool opt-out checks, phase-completion `[REVIEW]` append), `templates/playbooks/tools.md`, `templates/tools/README.md`, `templates/agents/claude-code/permission-baselines.md`, `templates/prompts/seed-harness-sync-marker.md.template`, `docs/tool-integration-architecture-analysis.md`. **Fourth fold:** the consumer sweep surfaced `inconsistencies.md` (assessment-phase ranked findings, scaffolded by onboarding, never even listed in CLAUDE.md's tree) as duplicating the "assessment findings" concern with `assessment.md`; it folds into `assessment.md` as a `## Inconsistency Report` section, keeping the canonical set at eight. Closes the additive-ratchet finding from the 2026-06-06 orchestrator-state retrospective: every harness pipeline acquires state slots, none sheds them. Files folded by function; zero lost lookups. The pre-clear reconciliation gate models the orchestrator context window as a write-back cache over the durable files and makes "is it safe to /clear" an explicit reconstruct-and-diff rather than a feel call. Before/after function-preservation (the baby-not-thrown-out proof):

| Retired file | Function | Destination | Lookup preserved by |
|---|---|---|---|
| `decisions.md` | dated decision + rationale | `journal.md` entry tagged `[DECISION]` | `grep '\[DECISION\]' journal.md` |
| `open-questions.md` | current unresolved questions | `orchestrator-state.md` `## Open Questions` section | surfaced on the dashboard every session (read first) |
| `review-history.md` | longitudinal reviewer findings | `journal.md` entries tagged `[REVIEW]`; rolling counts in dashboard "Review Tracking" | `grep '\[REVIEW\]' journal.md` + dashboard summary |
| `inconsistencies.md` | assessment-phase ranked findings | `assessment.md` `## Inconsistency Report` section | same assessment artifact as the checklist |

  Authored under `openspec/changes/orchestrator-state-consolidation/` (proposal + design + tasks). Historical CHANGELOG entries, archived/intake OpenSpec proposals, and OpenSpec change-proposal-level `decisions.md` files are deliberately not rewritten. Out-of-scope retrospective findings (cross-container machinery, harness-sync simplification, leak-layer dedup, regime taxonomy, the harness-maintainer role) deferred to separate proposals.

- **OpenSpec close-out playbook: Verification-Status Preservation guard added** (`openspec/AGENTS.md` § "Verification-Status Preservation" subsection inserted between Mechanical close-out sequence and Commit convention; `templates/tools/openspec-setup.md` Step 1 embedded AGENTS.md content mirrors the addition; `templates/prompts/openspec-close-out-retrofit.md.template` carries the discipline in its close-out walking step). The canonical close-out playbook now distinguishes design intent from verified ground truth for spec systems that carry verification-status frontmatter or markers (a convention encouraged by the archaeologist persona's `[verified]` / `[unverified]` tagging; adopted by some projects as a structured field like `archaeologist_verified: <ISO date>`). When applying spec deltas: verification-status fields stay untouched (close-out does not re-verify the live system); each applied section gets a provenance marker like "reflects design intent, not yet re-verified against the live system." Conditional discipline -- no-op for spec systems without verification-status fields. Caught and worked around locally by a peer AEH orchestrator session during the first real exercise of the convention, lifted to the canonical playbook so subsequent close-outs inherit the guard. Closes the verification-status preservation gap captured at commit da4b704 and triaged at commit b43bea9-successor.

- **Orchestrator response end-state vocabulary expanded from 3-state to 4-state** (`templates/personas/orchestrator.md` § "Response End-State Discipline" replaces `DONE | DECISION-NEEDED | NEXT-STEP-CLEAR` with `DONE | DECISION-NEEDED | MONITORING-BACKGROUND | PAUSED-ON-YOUR-WORK`; one-line definitions for each state and the choice-vs-result rule-of-thumb added; "no fifth state" closing-discipline preserved). Every orchestrator response now communicates two facts simultaneously in its closing label: is the clock ticking somewhere, and on whose side. `MONITORING-BACKGROUND` covers the case where a chain wrapper / scheduled wakeup / autonomous loop is running on the orchestrator's side (operator can walk away); `PAUSED-ON-YOUR-WORK` covers the case where an off-screen action is on the operator's side and the orchestrator is idle until the operator returns with a result. The semantic distinction between `DECISION-NEEDED` and `PAUSED-ON-YOUR-WORK` is choice vs result: the first needs the operator to pick among options the orchestrator framed; the second needs the operator to produce some output the orchestrator was waiting on. The previous 3-state vocabulary collapsed both ticking-clock cases into the catch-all `NEXT-STEP-CLEAR`, losing useful signal about who owns the next move. This is the first content captured through the new harness capture inbox (provenance file in `openspec/changes/orchestrator-end-state-vocabulary-v2/provenance.md`) and demonstrates the mechanism end-to-end on inaugural use.

- **OpenSpec adoption for the harness itself (dogfooding)** (`openspec/project.md`, `openspec/specs/README.md`, `openspec/changes/README.md`; `CLAUDE.md` Harness Maintenance Discipline gains "OpenSpec for substantive harness changes" + "OpenSpec authoring is target-detail-free" rules + Project Structure tree updated; `templates/personas/harness-reviewer.md` Dimension 1 extended to cover `openspec/**` explicitly). The harness now manages its own substantive changes through OpenSpec proposals under `openspec/changes/<slug>/`, mirroring the discipline it prescribes for target projects. Trivial changes (typos, ASCII fixes, single-file cosmetic updates with no rule/behaviour change) bypass OpenSpec via `[trivial]` / `[hygiene]` commit-message prefixes -- the OpenSpec layer is a tool, not ceremony. The spec corpus under `openspec/specs/` is intentionally empty at adoption time and grows organically as proposals archive; no retrofit of pre-adoption capability is planned. Authoring discipline: proposals and specs are public, target-detail-free, authored from scratch rather than copied from local triage scratchpads (`BACKLOG.md`, `*.private.md`, `*.local.md`); the publication gate catches pattern-matched leakage in `openspec/**` automatically and the harness-reviewer's Dimension 1 catches paraphrase-class leakage by judgment. Closes the "no methodical surface for harness-level change proposals" gap that surfaced when planning the first multi-step harness improvement (cross-container shared-workspace isolation) and recognising that ad-hoc edits + CHANGELOG entries alone were insufficient continuity infrastructure for any change spanning more than one session or eventually attracting external contributors.

### Security
- **Leak-detector hardening + Publication Gate + review-intermediary discipline** (`bin/validate-personas.sh` redesigned; `bin/.leakage-patterns.example` added; `bin/.leakage-patterns` gitignored; `templates/personas/orchestrator.md` new "Publication Gate" + "Review Intermediaries Are Local-Only" sections; `templates/personas/harness-reviewer.md` Dimension 1 broadened; `templates/personas/reviewer.md` review-intermediary discipline added to section 3; `templates/hooks/` pre-commit and pre-push templates shipped; `CLAUDE.md` Harness Maintenance Discipline key-rules block extended; `.gitignore` extended with `comments.md`, `docs/reviewer-orchestrator-upgrade-notes.md`, `*.private.md`, `*.local.md`, and `bin/.leakage-patterns`). Closes a publication-discipline gap where the previous leak-detector hardcoded real identifiers in its tracked source (the detector itself published the names it was meant to catch); the blocklist was incomplete (missed several known target slugs, allowing those identifiers to slip into `CHANGELOG`, `comments.md`, and `docs/reviewer-orchestrator-upgrade-notes.md`); the detector scanned only `templates/personas/` and therefore could not catch leakage in `CHANGELOG.md`, `README.md`, `docs/`, or commit messages; the harness-reviewer trusted the detector's verdict and produced "clean" reports against incomplete coverage; review-intermediary artefacts were tracked rather than kept local-only, accumulating target detail in the public repo; and `.gitignore` alone failed to protect files that were already tracked (`gitignore != untrack`). Fix: detector blocklist sourced from `bin/.leakage-patterns` (gitignored, populated per environment) with a placeholder `.example` committed; detector now scans CHANGELOG/README/CLAUDE.md/docs/templates broadly; new `--staged` and `--message` modes for commit-time gating; orchestrator gains a non-negotiable Publication Gate that runs both modes before every harness commit and push; harness-reviewer Dimension 1 extended to commit messages, the reviewer's own output, the validator-blocklist-presence precondition, and "tracked review intermediary is itself a finding"; reviewer base template's section 3 codifies review intermediaries as local-only with per-review dated reports under `docs/AE/reviews/` as the durable form; CLAUDE.md key-rules add the Publication Gate, the private-blocklist rule, the local-only review-intermediary rule, and the explicit "gitignore != untrack" reminder; pre-commit and pre-push hook templates shipped under `templates/hooks/` for defence-in-depth at the git level. Git history concurrently rewritten to purge two named review intermediaries plus five gitignored-but-historically-committed planning documents containing the same leakage class; commit messages with target identifiers were rewritten via `git filter-repo --replace-message`. Mirror backup retained pre-rewrite.

### Changed
- **Context7 default install is now CLI + Skills (preferred), with the MCP server demoted to a documented fallback; setup is functionally gated at onboarding completion and health checks** (`templates/tools/context7-setup.md` full rewrite; `templates/tools/context7-teardown.md` dual-mode; `templates/tools/tool-detection-patterns.md` Context7 detection + smoke-test rows; `templates/tools/README.md` Context7 row; `templates/playbooks/tools.md` offer + Phase 4 env-provisioning note + error-handling rows; `templates/playbooks/onboarding.md` Phase 6g offer block + 6h made MCP-only + new Phase 6j Standard-Tool Verification Completion Gate + Phase 4 step-c condition; `templates/playbooks/health-check.md` § 3i mandatory functional verification + configured-tool static-check note; `templates/governance/assessment-checklist.md` item 9.8; `templates/governance/review-criteria.md` § 5 scope note + common-problems list; `templates/personas/developer.md` § library-currency (dropped the "it is an MCP server in `.mcp.json`" assertion); `templates/personas/harness-reviewer.md` context7 audit criterion (was asserting MCP-in-`.mcp.json` as the correct description -- would have flagged the new template as wrong); `templates/agents/README.md` tool-knowledge row; `templates/tools/sandbox-env-provisioning.md` passthrough-var note; `docs/tool-integration-architecture-analysis.md` forward-pointer update note; `README.md` prerequisites). Context7 can now be installed via `ctx7 setup --cli --<agent>`, which drops a user-global docs skill + always-on rule into the agent's config (`~/.claude/skills/`, `~/.claude/CLAUDE.md` rule); the agent then autonomously runs `ctx7 library` then `ctx7 docs`. This mode needs no `.mcp.json` and no mandatory `CONTEXT7_API_KEY` (a key only raises rate limits), removing the `.env`/sandbox-passthrough machinery from the default path -- onboarding Phase 6h now fires only for the MCP fallback. Because the skill is user-global it leaves nothing in the project tree, so "configured" can no longer be verified by reading a project file: a functional smoke test (`ctx7 library react "..."` then `ctx7 docs /facebook/react "..."` returning content) is now the proof. That smoke test is wired into two hard gates per the operator's request -- a new onboarding Phase 6j (onboarding is not complete until Context7 is proven working or explicitly opted out in `decisions.md`) and health-check § 3i (a `configured` status with a never-run or failing smoke test is a HIGH finding). The MCP path is retained verbatim as a fallback for environments that can't run the `ctx7` CLI (no Node 18+ / no npx) or that specifically want an MCP-native tool. Closes the gap surfaced 2026-05-30 during a guinea-pig onboarding where Context7's CLI+skills install mechanism (not the MCP wiring the templates assumed) was the actual path, and where a `configured` profile line could stand in for a tool that was never proven to return docs.

- **Harness-reviewer + health-check now enforce the two onboarding defaults** (`templates/personas/harness-reviewer.md` Dimension 2 + Dimension 10 + `comments.md` table additions; `templates/playbooks/health-check.md` § 3i Standard-tool presence subsection + new § 3m Prompt Delivery Health + Phase 4 delta report table additions + Terminal Summary line items). Harness-reviewer Dimension 2 now checks `CLAUDE.md` documents `direct` as the harness default, the orchestrator carries the Path Invariant + pre-handoff self-check, and the `manual` opt-out path documents the `cp`-or-inline requirement. Dimension 10 now checks the onboarding playbook defaults OpenSpec + Context7 to in-scope (opt-out, not opt-in), the greenfield short-circuit runs the default-install blocks, and the tools playbook framing rule distinguishes AEH-standard from optional. Health-check § 3i grows a standard-tool-presence subsection that flags OpenSpec or Context7 absence (HIGH if no `decisions.md` justification, informational otherwise) so a target onboarded under the old "ask" flow is surfaced as missing the AEH-standard substrate. Health-check § 3m is new and verifies (a) `policy: direct` (or `manual` with `decisions.md` reason), (b) every harness-side prompt has a target-side mirror (catches silent-mirror-failure), and (c) no `Read and execute targets/...` or `/workspace/aeh/...` anti-pattern entries in journal/state. Both new dimensions feed into the Phase 4 delta report and the terminal summary count. Closes the gap where the two onboarding-default changes were enforceable at onboarding time but invisible to recurring health checks; without this, projects onboarded before the defaults shipped would silently retain the broken state and accumulate technical debt without anyone noticing.

- **Prompt delivery defaults to `direct` (harness writes target-side too); orchestrator handoff path is now an explicit invariant** (`CLAUDE.md` § "Selective exception: Direct Prompt Delivery (default)"; `templates/playbooks/onboarding.md` Phase 2 greenfield profile template + Phase 2a brownfield profile checklist + Phase 3 brownfield reconnaissance subsection; `templates/personas/orchestrator.md` § "Prompt-Write-Then-Handoff -- Hard Rule" gains a "Path Invariant" subsection and a 4-step pre-handoff self-check). Previously the playbook asked the operator to choose `direct` or `manual` during onboarding with no default; under `manual`, prompts lived only at `targets/<slug>/prompts/` and the orchestrator's handoff one-liner -- which always names a target-side path because the target Claude session is filesystem-scoped to the target tree -- pointed at a path the target could not read. Brownfield onboarding sessions repeatedly produced "Read and execute /workspace/aeh/targets/<slug>/prompts/..." handoffs that broke on arrival; the operator manually corrected each time. Fix: `direct` is the default for every target (set automatically in `profile.md` without asking); the orchestrator's hard rule now states the handoff path is ALWAYS target-side (`docs/AE/prompts/NNN-title.md`), naming "Read and execute targets/..." as a broken-on-arrival anti-pattern by example; a pre-handoff self-check verifies the mirror landed; the `manual` opt-out path requires the orchestrator to ship a `cp` command alongside the handoff or inline the prompt content -- never just point at the harness path. Engineering personas already use the target-side path in their handoff examples; this change closes the upstream defect where the policy mechanism let manual mode produce broken handoffs.

- **OpenSpec and Context7 are now default in-scope during onboarding (opt-out, not opt-in)** (`templates/playbooks/onboarding.md` Phase 6g + greenfield short-circuit step 9b; `templates/playbooks/tools.md` intro + framing rule + Phase 3 offer language; `templates/tools/README.md` design principles + Available Tools status column; `CLAUDE.md` `tools` command row). Previously Phase 6g presented OpenSpec as `[yes / not now / never]` and Context7 as `[yes / not now]` -- both opt-in by default. Brownfield onboarding sessions repeatedly surfaced the failure mode where the operator (focused on the project work) treated the offer as "maybe later" and ended up with a target that lacked the load-bearing spec substrate + library documentation lookup, only to discover the gap weeks later when developer prompts hit unknown library APIs or reviewer §0 spec traceability had no spec to trace to. Fix: both tools now present a `[Y (default) / defer / opt-out]` block during onboarding and via the `tools` playbook; the default path is installation; explicit opt-out captures the operator's reason in `decisions.md`. Serena remains genuinely optional (codebase-dependent assessment unchanged). The engineering persona base templates already named OpenSpec and Context7 as standard SDLC tools per the harness-reviewer dimensions 8 + 10; this change aligns the onboarding flow with that long-standing template-level assumption. Closes a recurring operator friction surfaced 2026-05-30 during a brownfield onboarding review where OpenSpec had been "offered as optional post-onboarding" and the project subsequently lacked spec scaffolding.

### Fixed
- **Target CLAUDE.md session-init banner now bypassed on dispatch-prompt invocation** (`templates/project/CLAUDE.md.template` § "On first message of every session" -- a new "Dispatch-prompt invocation -- skip the banner entirely" exception clause inserted before the numbered banner-flow steps). When the first user message of a fresh target session matches `Read and execute docs/AE/prompts/...` (or a tool-call reading a file at that path is the very first action), the target CLAUDE.md no longer outputs the banner or waits for confirmation -- the prompt file's Step 0 is the session initialiser (it overwrites the persona marker authoritatively and loads the persona files itself). The banner remains for interactive role-choice sessions. Previously, the banner ran on every first message regardless of intent, including when a stale persona marker from the prior session collided with a role-bound dispatch prompt; the banner would render "[role] from last session -- continue or switch" and wait, blocking the prompt's Step 0 from silently switching to the prompt's required role. Recovery required the operator to type "ignore role" or "continue" before re-pasting the dispatch line -- friction the prompt's Step 0 was supposed to eliminate. Closes a drift surfaced 2026-05-20: a target banner-stalled on two consecutive dispatch prompts despite both carrying a proper Step 0; the orchestrator persona template's "drift trap" subsection named this as a target-CLAUDE.md defect to fix structurally and queued the template change. This commit ships the structural fix to both the harness template and any active target that adopts it. Pairs with the existing `feedback_handoff_explicit_role_switch` memory rule: prompt owns the role-switch via Step 0; operator-typed switching is recovery, never handoff.

- **Report-Back marker examples were Unicode em-dash; ASCII-fix + paste-mandatory snippet to harden the convention** (`templates/personas/orchestrator.md` § "Report-Back discipline"). The opening marker (`PROMPT REPORT`) and closing sentinel (`PROMPT COMPLETE`) example blocks and inline named examples used Unicode em-dash `—`, which violates the harness-wide ASCII-only rule (`CLAUDE.md` § "Working Rules") and produces markers a target agent will emit literally with the Unicode glyph, breaking downstream grep/sed parsing in autonomous chain wrappers. All four example occurrences fixed to `--`. Additionally added a "Mandatory copy-paste snippet" block between the Discipline list and the Scope paragraph: a literal block the orchestrator pastes verbatim into every generated prompt's "Report back" step, so the opening-marker requirement cannot drift out of authored prompts via prose-only guidance. Closes a drift surfaced 2026-05-20: orchestrator authored six target-side prompts in a row (308 through 313) with the closing sentinel but no opening marker, leading the operator to defensively re-paste a target report because they could not tell which prompt it belonged to. Root cause: the previous template stated the marker discipline in prose only; without a paste-mandatory snippet, the prompt author had to remember to add the opening marker to each prompt's "Report back" step from memory.

- **Layered-persona loading -- AEH base persona templates are now copied into the target tree so target sessions can load them** (Q-08; `templates/playbooks/onboarding.md`, `templates/personas/orchestrator.md`, `templates/playbooks/health-check.md`, `templates/project/CLAUDE.md.template`). The layered-persona model said the generic base template lived in the harness at `templates/personas/<role>.md` while the overlay lived in the target at `docs/AE/personas/<role>.md`. A target-side Claude is filesystem-scoped to its own project tree and cannot read the harness path, so a role could never actually load its base methodology -- and because onboarding prompts are freestyle (no role), onboarding never exercised a role and the gap stayed invisible. Fix: base templates are now copied into the target tree at `docs/AE/personas/_base/<role>.md` (a per-target snapshot of the harness base), overlay `AEH Base:` headers reference that target-side path as the operative load path, onboarding generates a base-template-placement prompt (Phase 2) carrying the five base templates inline, and onboarding gains a hard role-activation completion gate (Phase 4, section 6i) -- onboarding cannot be declared complete until one trivial role-bound prompt proves a role loads both files from within the target tree. Freestyle prompts now carry a marker-clearing freestyle Step 0 so a stale role marker does not stall them. Health check gains a Role Activation check verifying `docs/AE/personas/_base/` is present and overlay headers point target-side.

- **Onboarding Phases 4-5 no longer conflate inconsistency findings with the onboarding work list** (`templates/playbooks/onboarding.md` -- Phase 4 gains a "4a. Scope boundary" subsection; the closing "which severity levels do you want to address" question is removed; the Phase 5 plan-generation text and example task list are rewritten to be unambiguously AEH-skeleton-only). Previously Phase 4 closed by asking the operator which severity levels to "address", and the Phase 5 example plan carried `[CRITICAL]`/`[HIGH]`-tagged remediation tasks ("Resolve contradictory instructions") interleaved with harness-setup tasks. Both implied the onboarding plan fixes assessment findings -- but Phase 6 is explicitly "AE Harness Setup Only" and finding-remediation touches application code/config/non-AE docs, which is downstream archaeologist and reviewer-implementer work. Fix: 4a states the inconsistency report is a handoff artifact, not a work list, and that no severity-scoped decision is taken until the Phase 7 option choice; Phase 5 now declares the onboarding plan is always the harness skeleton, never carries severity tags, and ships a skeleton-only example (CLAUDE.md merge, docs/AE/ structure, persona overlays, tooling, regression + retrospective). Closes a derailment surfaced 2026-05-15 during onboarding of a brownfield target: the operator caught the harness asking a severity-scope question that implied onboarding would modify target code/specs.

- **OpenSpec setup no longer surfaces npx / Node CLI step** (`templates/tools/openspec-setup.md` -- removed numbered step 2 `npx openspec init --tools claude`; rewrote "MCP Server: When to Use It and When Not To" section into "OpenSpec for Claude Code = a directory convention" with explicit "Do NOT introduce Node, npm, npx, or the OpenSpec CLI as part of AEH setup" rule; demoted MCP server section to an appendix flagged as not part of AEH-generated prompts; renumbered remaining setup steps 1-3). Closes a confusion surfaced 2026-05-12 in a colleague's first onboarding: the generated setup prompt's "Step 2: Initialise OpenSpec (if the CLI is available) -- npx openspec init --tools claude" caused the operator to ask whether installing npx was required, despite the directory-convention model making the CLI entirely irrelevant for Claude Code. Root cause: the template hedged the CLI as an "if available" option in the main numbered flow, treating its absence as a skipped step rather than a non-step. New framing: for Claude Code, OpenSpec is two directories + a CLAUDE.md section, full stop; the CLI and MCP server exist for other use cases (sandboxed agents, manual human validation) that AEH onboarding does not cover.

- **Greenfield short-circuit dropped Phase 6g/6h tool setup** (`templates/playbooks/onboarding.md` greenfield short-circuit step 9 rewritten; step 8 plan template clarified to mark OpenSpec/context7 as operator-choice and Serena as auto-skipped on greenfield with re-assessment via `tools`). Previously step 9 said only "generate the standard skeleton prompts" and stopped there, so although the standard greenfield plan listed OpenSpec and context7 as steps 8 and 9, neither offer block ever fired -- no setup prompt was generated, `profile.md` left `policy: TBD` / `context7: TBD` permanently, and 6h sandbox env provisioning was unreachable. Brownfield onboarding did this correctly via 6g/6h; greenfield silently dropped it. Fix: step 9 now decomposes into 9a (skeleton prompts), 9b (mandatory 6g run -- OpenSpec offer, context7 offer, Serena auto-skip on greenfield codified explicitly), 9c (6h sandbox env if context7 accepted). Closes a defect surfaced 2026-05-11 in a colleague's fresh-clone onboarding of a target project where the target-side analyst reached the kickoff and noticed OpenSpec was absent despite being in the approved plan.

### Fixed
- **Step 0 role activation must suppress the session-init banner and never present a role-choice quiz** (`templates/personas/orchestrator.md` § "Layered Persona Loading" -- the Step 0 rendering guidance and the Step 0 block template both strengthened). The Step 0 block already said "self-activate the role" and "overriding whatever persona was active", but it never told the target to suppress the target CLAUDE.md session-init banner / role picker. Result: when a role-bound prompt was pasted into a fresh target session, the CLAUDE.md "show banner, wait for confirmation" behaviour and the prompt's Step 0 collided, and the target rendered a five-option role-choice quiz instead of just switching. Fix: the rendering guidance now states the Step 0 switch is unconditional and silent (hard rule) -- it must explicitly suppress the CLAUDE.md banner/role-picker, never render a numbered role-options list or "role info" menu, never ask the operator to choose/confirm a role, and treat any carried-over persona as irrelevant; a prompt that lets the target present a role-choice quiz is defective. The Step 0 block template itself gains an opening directive ("Step 0 IS this session's initialisation; do NOT run the CLAUDE.md banner... enact the role, do not deliberate it") and a closing "No banner, no menu, no question." Closes a derailment surfaced 2026-05-15: a regression-check prompt pasted into a fresh session whose carried-over marker read `developer` produced a role quiz rather than switching to the prompt's declared `reviewer` role.

### Added
- **Report-Back opening marker (report bracketed top and bottom)** (`templates/personas/orchestrator.md` § "Report-Back discipline" -- the section's two mandatory conventions become three). Every generated target-side prompt must now instruct the agent to open its Report Back with a `PROMPT REPORT — <identifier>` marker as the literal first line, in addition to the existing wall-clock field and closing `PROMPT COMPLETE — <identifier>` sentinel. Opening marker and closing sentinel together bracket the report with an identical prompt identifier; a mismatch between the two is a flagged defect. Rationale: in sessions that run several prompts back-to-back (batch drivers, chains, long operator sessions), report outputs interleave with recaps and tool noise, and a report that opens mid-prose gives the orchestrator no anchor for which prompt it is assessing. Closes a recurring fade: the top-anchor idea was raised 2026-04-18 and lived only in operator-memory, then faded from prompt-authoring habit; re-flagged by the operator 2026-05-15 with the instruction to codify it in the template (the single control point that authors all prompts) so it stops fading.

- **Orchestrator prompt-write-then-handoff hard rule** (`templates/personas/orchestrator.md` new subsection "Prompt-Write-Then-Handoff -- Hard Rule" inserted in the Prompt Handoff Protocol section). Codifies that every substantive instruction the target session acts on must be written to a prompt file BEFORE handoff, and that a chat fenced block may only ever contain a `Read and execute docs/AE/prompts/NNN-title.md` pointer or verbatim operator-local commands -- never an improvised multi-step directive. Names the anti-pattern explicitly: improvising a batch driver / "do X then Y then report" instruction into the chat instead of a file. States that a batch driver sequencing other prompts is itself a prompt file. Provides the single exception (operator explicitly suspends the rule, per-instruction and temporary) and the recovery behaviour (name the slip, write the file, re-issue). Closes a discipline gap surfaced 2026-05-15: the orchestrator handed an ad-hoc five-prompt sequencing instruction to the operator as an inline fenced block instead of a committed batch-driver prompt file; the operator caught it. The handoff section already said "never describe what to do in prose -- give the exact text", but that wording did not forbid exact text that is itself an un-filed orchestration instruction.

- **Greenfield onboarding short-circuit** (`templates/playbooks/onboarding.md` new "2.0 Greenfield Detection (run first)" + "Greenfield Short-Circuit" sections inserted at the top of Phase 2; new working rule "Onboarding is structural, not domain-discovery" added to `CLAUDE.md` Working Rules). Codifies that empty repos and default-README-only repos are the easiest onboarding case, not a blocker. The harness detects greenfield state via five fast checks (zero source files, no manifest with content, no agent config, no CI/CD, README is default-template-only) and short-circuits straight to skeleton generation: skips reconnaissance subphases 2a-2d, assessment subphases 3a-3d, and Phase 4 reporting; writes `profile.md` with `Stack: TBD`, `Domain: TBD`, `Team: TBD` placeholder fields; runs a standard greenfield plan identical for every greenfield target (CLAUDE.md + 5 persona overlays scaffolded with header-only `## Project Identity: TBD -- populated by analyst on first feature` + standard OpenSpec/context7 + regression + retrospective). The CLAUDE.md working rule names the decoupling explicitly: skeleton generation is independent of project content; persona overlays scaffold with placeholders and are populated by the analyst on first feature, not by the onboarding playbook. Closes a trajectory-drift bug surfaced 2026-05-11: a colleague's fresh-clone onboarding of an empty GitLab repo stalled because the harness Claude conflated "assessment" with "domain understanding", started interviewing the operator about domain/stack/team, and produced no skeleton files in either the harness `targets/` directory or the target's `docs/AE/`. Root cause: Phase 2's detection tables are heavily brownfield-biased (look for personas, MCP servers, OpenSpec dirs, etc.); when every probe returns empty there was no explicit "this is greenfield, proceed to skeleton mode" branch, so Claude improvised an interview. The fix decouples the two concerns the architecture already separates (generic base templates + project-specific overlays) but the playbook previously did not signpost clearly enough for an LLM under ambiguity.

- **Orchestrator closure-prompts-verify-post-push-pipeline discipline** (`templates/personas/orchestrator.md` new section "Closure Prompts Must Verify Post-Push Pipeline" inserted after "No Deflection of Runnable Work"). Codifies that any orchestrator-authored prompt whose mission includes a push triggering CI MUST include a post-push pipeline-verification step asserting the pipeline reached an acceptable terminal state for the work shape (closure / hygiene / verification gate / docs-only). Names the anti-pattern explicitly: closure-shaped prompts framed as "pipeline outcome is informational" can ship a CP as delivered while CI is red, allowing the substantive bug the closure was meant to deliver against to never reach the deployed environment. Closes a discipline gap surfaced 2026-05-06: prompt 285 (qa-smoke-plus round-1) shipped with that exact anti-pattern; the post-push pipeline failed at e2e + skipped deploy_QA; closure (286) declared CP delivered without catching the red CI; the F-001 outage that round-1 was supposed to surface kept running for 17 more hours. Lift trigger met when F-001 was durably closed via 290d (deploy_QA green end-to-end on `ad1ebbd`).

- **Analyst Flight-Level Product View (Product-Manager mode)** (`templates/personas/analyst.md` new section §7c "Flight-Level Product View" inserted after §7a QA Capture Mode + new §7c.PROJECT extension point). Codifies a second analyst altitude alongside the default BA-detail mode: aggregates across OpenSpec corpus + BACKLOG + in-flight CPs + reviews + e2e outcomes to produce four artefacts -- functionality coverage matrix, use-case coverage map, cross-cutting concerns view, vision-delta narrative. Modes alternate (operator-triggered explicitly via "give me the flight-level view" or similar); they do not run concurrently. Boundary with Orchestrator made explicit: Analyst's Flight-Level handles Product Management (what/why/sequence); Orchestrator handles Project Management (pipeline/blockers/dispatch). Operator remains the actual PM with vision-and-priority authority; Flight-Level augments operator thinking with structured views, never replaces it. Closes a capability gap surfaced 2026-05-06: previously the operator had to mentally aggregate multiple files to construct the product-state picture; now the Analyst produces it as a structured deliverable. Tool-agnostic at harness level; project overlay (§7c.PROJECT) names domain-specific category lists, journey lists, cross-cutting concerns, and the deliverable path convention.

- **Orchestrator no-deflection-of-runnable-work discipline** (`templates/personas/orchestrator.md` new section "No Deflection of Runnable Work" inserted after "Paste-Handoff /clear Discipline"). Codifies that runnable commands belong to the orchestrator (harness-side) or target-agent-via-prompt (target-side), never to the operator. Operator runs commands manually only for genuine human-in-the-loop work: cloud-console interactions, identity-authorised network changes, runtime-judgement decisions, one-time credential bootstrap, and operator-only-access ops. Anti-pattern named explicitly: "from the target terminal, run git push origin main" -- this is runnable by a target prompt; deflecting it to the operator is wrong. Even one-liner pushes get the prompt-file treatment per audit-trail discipline. Closes a discipline gap surfaced 2026-05-06: orchestrator deflected a target-side git push to the operator; operator pushed back that git work belongs to the orchestrator (via target prompt) or the target agent, never to them.

- **Orchestrator paste-handoff /clear discipline** (`templates/personas/orchestrator.md` new section "Paste-Handoff /clear Discipline" inserted after "Response End-State Discipline"). Codifies the four-step decision checklist for when to recommend a context clear before a target-paste (role switch / domain change / halt-resolution-never / default-preserve) and the exact phrasing `/clear first` as a standalone literal directive (not conversational hedging). Closes a discipline gap surfaced 2026-05-06: orchestrator drifted between over-recommending /clear and conversational phrasing the operator could not reliably scan. Operator scans for the literal `/clear first` string -- its absence is itself the signal that no clear is needed.

- **Orchestrator response end-state discipline** (`templates/personas/orchestrator.md` new section "Response End-State Discipline" inserted between "Mission Ownership" and "Before You Start"). Codifies that every orchestrator response must end in exactly one of three states: DONE (nothing queued, said explicitly), DECISION-NEEDED (options + recommendation + rationale + ask), or NEXT-STEP-CLEAR (target-paste-string OR operator-local verbatim commands). No fourth state; internal harness work happens during the response, not as a deferred end-state. Closes a discipline gap surfaced 2026-05-06 where orchestrator turns drifted into ambiguous endings ("let me know" / "we'll see") that created cognitive load and stalled the operator's parallel-context management. Source: operator-stated rule captured to memory + lifted to template same session.

### Changed
- **README full restructure -- governance harness for mature SDLC needs** (`README.md` 342 -> ~210 lines net). Repositions AEH from "Agentic Engineering Harness" to "Agentic Engineering Governance Harness" (G as clarifier, not a rename). Restructured into question-answering sections in this order: "What is this?" (lead with positive category disambiguation from democratised AI app builders, oriented toward "mature, enterprise-grade SDLC needs, principles, and requirements"; addresses the "AI magic = fire engineering function" reader without condescension); "What AEH is not" (factual category-clarification block, moved up to sit in the orientation block at the top); "Why this exists"; "Inner mechanics" (seven SDLC discipline bullets + a paragraph on the relationship to Jira and agile workflows -- vocabulary overlap, what OpenSpec does not compete with, integration points if Jira is brought in, load-bearing point that Jira integration does NOT mechanically change AEH workflow); "The roles" (engineering personas table with key-discipline column + coordinating roles table + paragraph on the loop and PASS/WARN/FAIL/BLOCK verdicts and the two-session shape); "Operation modes" (conversational / operator-paced / autonomous chained spectrum, with operator-paced explicitly named as the review surface where dialogue or direct prompt-file edits happen); "Onboarding modes" (greenfield vs brownfield + on-ramps table); "Required infrastructure" (lead with operator-safety framing -- AEH does not protect from rogue AI actions, your job to create a safe agentic coding environment; if seasoned, do it; if not, find help -- followed by the technical sandbox + control-in-depth paragraphs); **NEW "Prerequisites" section** listing the concrete keys / credentials needed before `claude` + `onboard` (coding-agent runtime, source-control creds, context7 API key, OpenSpec scaffolded by onboarding, project-specific creds in your own env); "Quick start"; **NEW "Where things live" section** with two clearly-labelled subsections -- "In your project (after AEH onboarding)" naming the OpenSpec layout + the docs/AE/personas + docs/AE/prompts (handover point) + docs/AE/reports + docs/AE/reviews + AGENTS.md + target-side CLAUDE.md, and "In this harness repo" with the existing harness-side pointers relabelled to be unambiguous; footer. Adds three diagrams: system-overview (operator + orchestrator + 4-role team + outputs) embedded in the "What is this?" section; end-to-end pipeline (operator -> AEH -> GitLab -> deployment + documentation -> live product) embedded after the "Why this exists" section; prompt-execution loop (mandate -> execute -> report -> correct) embedded in "The roles" section. Adds new "Documentation portal (optional but recommended)" section after "Where things live" describing the MkDocs + Material publishing layer for the structured markdown AEH produces (specs, change proposals, reports, reviews, ADRs, persona overlays); covers what it gives you, what it requires (Python + mkdocs + mkdocs-material + mkdocs.yml + a CI job + optional plugins for mermaid / broken-link checking / openspec-mirror); where it becomes available (GitLab Pages, GitHub Pages, self-hosted); and the load-bearing point that AEH does not scaffold MkDocs but the documentation it produces is naturally suited to that publishing layer. Voice: concrete, observational, not promotional. Self-policed for the "not A; it is B" rhetorical trope. ASCII-only throughout per the harness rule.

### Changed
- **Chain-wrapper halt sentinel: delimited form `<<<CHAIN_HALT>>>` replaces bare `CHAIN_HALT`** (`templates/personas/orchestrator.md` §"Multi-prompt Multi-role Chain Orchestration -> Halt condition catalogue" -- bullet for the sentinel updated 2026-05-04). Closes a wrapper false-positive that bit a real chain on 2026-05-04 (theming CP Tasks 3-4 chain): the wrapper's `grep -q "CHAIN_HALT" "$SESSION_LOG"` matched the literal string in the prompt body's halt-protocol instructions ("emit `CHAIN_HALT` on stdout"), causing a false halt between prompts even though no actual emission occurred. Same pattern was present in all chain wrappers used this session; just had not yet bit because previous chains either single-prompt (no inter-prompt step gated on the grep) or had real halts overlapping the false positive. Fix: emission convention now uses the delimited form `<<<CHAIN_HALT>>>` -- a string unlikely to appear in prompt bodies; wrapper grep uses `grep -F "<<<CHAIN_HALT>>>"` for fixed-string match. Existing chain wrappers using the bare form remain functional as long as their prompt bodies never reference the literal in instructions; new chain wrappers (e.g., a target-side overnight closure-pair wrapper filed 2026-05-05) use the delimited form. Existing prompts referencing the bare sentinel may be left as-is until next-touch; new prompts emit the delimited form.

### Added
- **Calibration log discipline + data-driven heuristic table refresh** (`templates/personas/orchestrator.md` §"Report-Back discipline" subsection refreshed; new `targets/<slug>/calibration-log.md` per-target append-only convention; new `estimated_wall_clock_minutes:` frontmatter field on every role-bound prompt). Closes a discipline gap surfaced 2026-05-04: every target-side report already captures `Wall-clock: <start> -> <end> = <duration>` per the existing Report-Back rule, but estimates lived only as informal prose in prompts and there was no aggregated comparison anywhere. Memory note `feedback_prompt_wall_time_tracking.md` named "estimates 3-5x too high" without operationalising the recalibration. Now: (a) cross-project heuristic table refreshed against accumulated target data (Architect adjudication 5-15 min not 30-55 min; Developer mechanical 1-task application 1-5 min not 10-20 min; Developer mechanical multi-task 10-30 min; Developer diagnostic 10-25 min; Analyst surgical amendment 3-7 min added; iterate-to-green left near actuals because iteration count dominates); (b) per-target calibration log mandated as append-on-report capture so each target accumulates its own posterior distribution per shape; (c) `estimated_wall_clock_minutes:` machine-readable frontmatter field on prompts so future tooling can parse the dataset; (d) discipline rule that 3+ entries of a shape in the per-target log override the cross-project default. Initial calibration log filed for the active target with backfill of 11 prompts from the session 2026-04-29 to 2026-05-04, surfacing patterns: mechanical 1-task developer 10-20x over-estimated; architect adjudication 3-13x over; analyst surgical amendment 4-7x over; diagnostic developer 2-5x over; iterate-to-green 1.25-2x (closest).
- **Analyst commit discipline** (`templates/personas/analyst.md` new "Commit discipline" subsection inside §7 OpenSpec Integration, after the Handoff sub-section) -- closes a gap surfaced 2026-05-04 during the burnup-chart proposal initiative: the analyst persona had ZERO mentions of commit / git add / git commit, while every other engineering persona (architect, developer, archaeologist, reviewer-via-verdict-write) carried explicit commit discipline. Result: an analyst session ended with the produced proposal uncommitted on disk, and a follow-on session had to bundle the original work into the amendment commit -- losing the "v1 vs v2" diff in git history. New section establishes: commit the proposal as the final act of the analyst session; one commit per coherent unit (initial draft vs amendment); body tag `[change:<slug>]` for spec-traceability; path-scoped `git add`, no `-A`; subject line shapes for kick-off vs amendment; don't push (orchestrator handles dispatch authorisation); persona discipline takes precedence over a brief that forgot to name commit explicitly; surface uncommitted-on-halt explicitly so the orchestrator can route follow-up. Aligns analyst with the rest of the role family.
- **Orchestrator out-of-lane doctrine + expected pushback** (`templates/personas/orchestrator.md` §"Mission Ownership -- Do Not Deflect", new sub-block after the Operational-visibility rule) -- enumerates the role-engineering work that is OUT of the orchestrator's lane even when operator asks for it: reading failing test source code in detail to diagnose; diagnosing CI failures beyond surface-level "which stage failed"; editing application code/config/tests; re-triggering CI as part of iterate-to-green; running tests/lint/typecheck/build for debugging; authoring or amending design.md or change proposals (architect lane); authoring baseline or canonical specs (archaeologist/analyst lane); reviewing code or specs (reviewer lane). Names pushback as expected behaviour ("the pushback IS the dispatch -- not a refusal followed by a question"); pushback shape is concrete reroute (name the role, dispatch the prompt, surface the handoff). Draws the line: orchestrator coordination work is identifying obstacles + writing prompts + monitoring outcomes via APIs and state files; role engineering work is read-source / design / implement / test / debug / review / archive. The discipline applies symmetrically -- "monitor the pipeline" means polling status from a script, not reading the failing test's source. Lifted from a real-world session where the orchestrator drifted into developer-lane diagnosis (reading failing test code + auditService git diff in chat) before the operator caught it; doing the work pollutes the orchestrator context window and undermines the role-separation discipline AEH preaches.
- **ASCII-only output rule** (`CLAUDE.md` Working Rules section, new bullet after "No target project details in harness files") -- all generated content uses plain ASCII characters: response prose, markdown tables, prompt files, wrapper scripts, state files, commit messages, deliverables. No Greek letters, arrows, comparison glyphs, checkmarks, em/en dashes, ellipsis glyphs, smart quotes. Substitutions specified inline (`->` not Unicode arrow, `>=` not Unicode operator, `[x]`/`OK`/`PASS` not Unicode marks, `--` or `-` not Unicode dash, etc.). Reason: terminal and shell safety -- Unicode breaks in some terminal emulators, complicates `grep`/`sed`/`awk` pipelines, fragile under shell escaping, awkward in file names, gets mangled in copy-paste. Applies to all roles. Exception: do not ASCII-fy existing files unprompted; rule governs new content.
- **Orchestrator Mission Ownership doctrine** (`templates/personas/orchestrator.md` new §"Mission Ownership — Do Not Deflect", inserted after §"Role Boundaries — Do Not Cross") -- consolidates the operator/orchestrator authority boundary that was previously scattered across multiple sections. Codifies five ownership domains (pathfinding to the objective, task-list evolution, end-to-end prompt-output review, obstacle navigation, tooling/orchestration fabric including chain wrapper scripts). Names the **non-deflection rule** ("do, then report" as default; "ask, then do" only in enumerated cases) and provides explicit ALWAYS-ASK / DON'T-ASK lists for orchestrator actions. Adds a hard rule on **operational visibility** — every in-scope action surfaced in the response that follows it; silent state edits / file creates / script launches / commits are violations even when individually in-scope; operator stays in complete operational awareness without being required to micromanage. Lifted from real-world delivery where wrapper-script ownership (already present in §Multi-prompt chain orchestration as a narrow rule) was being deflected to the operator, surfacing the gap that the broader doctrine was implicit-only.
- **Persona marker Docker-awareness** (`bin/resolve-persona-marker.sh`) -- backward-compatible enhancement to the `.claude/persona` session-state convention. Resolver detects whether the session runs inside a Docker / container environment (via `/.dockerenv` or `/proc/*/cgroup` grep for `docker|containerd|lxc|kubepods`); if so AND `$HOSTNAME` is set to a non-trivial value, the marker resolves to `.claude/persona.$HOSTNAME` — giving each container its own marker when multiple AEH orchestrator sessions bind-mount the same harness directory. Otherwise falls back to `.claude/persona` unchanged (single-directory-no-Docker setups see no change). Includes opportunistic stale-marker cleanup: per-hostname markers untouched for >30 days are removed on each resolver call, so container-rebuild churn doesn't accumulate cruft. CLAUDE.md session-init + orchestrator persona Step 0 block updated to route through the helper. `.gitignore` extended to `.claude/persona.*` pattern.

### Changed
- **Harness-reviewer scope clarified vs AEH health-check** (`templates/personas/harness-reviewer.md` new "Scope clarification" section near top) -- explicit disambiguation between two review concerns that were running in parallel without a clean boundary. Harness Reviewer: reviews the generic harness layer (templates, personas, playbooks, governance) + surfaces lift candidates from target delivery. Health-check playbook: reviews a target project's AEH adoption depth (level, correctness, completeness, accuracy, tool health, coverage gaps per project phase). They share some signal sources but are not substitutes. Operators wanting to audit a target's adoption route to the health-check playbook; operators wanting to mature the generic harness invoke this persona. Resolves practical confusion surfaced during 2026-04-24 cross-project uplift work.
- **Orchestrator chain-launch authority formalised** (`templates/personas/orchestrator.md` §Multi-prompt Multi-role Chain Orchestration) -- orchestrator PROPOSES chain composition; operator AUTHORISES launch. Non-negotiable authority split. Autonomy is about the chain running without mid-chain operator interaction, NOT about the orchestrator launching multi-hour chains unilaterally. Proposal shape specified: scope + confidence rationale + halt conditions tuned for the chain + expected wall-clock + success-evidence description. Plus proactive-surfacing guidance (raise chain opportunities briefly in next-steps conversation, same cadence as writing the next prompt after a report) and trajectory-of-growth discipline (initial chains short + mechanically gated + high-confidence; grow chain ambition as per-project success data accumulates; skipping the confidence-building stage is an anti-pattern).

### Added
- **Orchestrator integration-verification gate between developer batch and reviewer** (`templates/personas/orchestrator.md` §Multi-prompt Multi-role Chain Orchestration → Integration-verification gate) -- distinct from both pre-flight readiness (pre-launch) and per-task mechanical gates (mid-chain). Fires AFTER a developer batch completes and BEFORE the reviewer prompt runs, arbitrating at real-integration level (real DB, real service wiring, real end-to-end test surface) that per-task unit tests intentionally mock away for speed. Shape: reset/provision clean environment → run real-integration suite → capture evidence → halt on failure (shortens feedback loop on integration bugs) or proceed to reviewer on pass. When-to-use / when-not-to-use criteria documented. Lifted from a second AEH project where this gate on first exercise caught 5 real integration bugs across two change-proposals that per-task unit tests had let through.
- **Orchestrator pre-dispatch hygiene gate** (`templates/personas/orchestrator.md` §Multi-prompt Multi-role Chain Orchestration → Pre-dispatch hygiene gate) -- cheap check the orchestrator runs BEFORE dispatching a prompt that opens a new forward change proposal. Verifies main-branch CI green, working tree clean, no unmerged long-running migrations blocking next CP's work. If red, halt CP dispatch and route to correction/cleanup first. Prevents cascading failure across CPs. Non-blocking waiver available when the forward CP is itself a CI-fix (operator override legitimate but audit-worthy). Lifted from a real AEH project delivery where skipping this check led to a CP dev batch diagnosing upstream issues rather than delivering its own scope.
- **Orchestrator PROMPT-COMPLETE sentinel requirement** (`templates/personas/orchestrator.md` §Generate Next Action → Report-Back discipline) -- every generated target-side prompt MUST end its Report Back section with `PROMPT COMPLETE — <identifier>` as the final line, after the wall-clock field. One line, canonical identifier matches prompt name. Load-bearing parse target for autonomous chain wrappers scanning session JSONL streams for clean-completion signal; without it, wrappers can't distinguish genuine completion from final-sounding text that didn't actually finish. Scope: all role-bound target-side prompts + interactive review prompts; orchestrator-session prompts and freestyle shell kickoffs skip the sentinel (not chain-consumed). The Wall-clock discipline subsection was renamed to Report-Back discipline to cover both conventions jointly. Lifted from informal usage across multiple AEH projects where the sentinel had become de-facto standard without being codified.
- **Harness-reviewer extended scan sources for lift-candidate surveillance** (`templates/personas/harness-reviewer.md` §Gather Evidence) -- when a harness-review's purpose includes surfacing lift candidates (patterns in a target project that belong in generic templates), the scan extends beyond harness templates + feedback memory to include: harness-side `targets/<slug>/decisions.md` and `targets/<slug>/review-history.md`, target-side `docs/AE/decisions.md`, `docs/AE/reports/`, and any existing `targets/<slug>/session-learning-report-*.md` artefacts. Many generic process rules live ONLY in these target-side files — they were decided during delivery, never promoted to a generic template. Scanning them surfaces lift candidates that feedback-memory + persona-template review alone misses. Instrumentation improvement flagged during a real harness-review pass.
- **Orchestrator multi-prompt multi-role chain orchestration** (`templates/personas/orchestrator.md` §Autonomous loop → Multi-prompt Multi-role Chain Orchestration) -- distinct from the single-prompt autonomous loop; covers proactive composition discipline for multi-prompt chains that string several prompts (potentially across multiple roles) into one autonomous wall-clock window. Names seven orchestrator responsibilities (identify / scope / pre-flight / guardrail / execute via wrapper / monitor / post-chain verdict); documents two proven chain shapes (same-role batch vs cross-role); enumerates chain-composition heuristics (when to chain, when not); catalogues halt conditions (non-zero exit, zero commits, reviewer ≠ PASS/WARN, CHAIN_HALT sentinel, mtime idle, wall-clock cap, scope-guard violation); preserves the orchestrator scope-guard that composition and monitoring are its lane, not engineering work. New `§CHAIN.PROJECT` extension point for project-specific wrapper scripts and halt sentinels. Lifted from real-world session-learnings driving autonomous chains that span hours of wall-clock with operator disengaged — done right, gives 8–15 hours of equivalent work per night; done wrong, amplifies failure across the same window.
- **Analyst two intake paths** (`templates/personas/analyst.md` §7b) -- distinguishes Path 1 (heavyweight operator-authored BA-REQ documents arriving at `docs/aeh-analyst-intake/` or equivalent, digested by the analyst with authority to reject / refine / decompose / defer / flag) from Path 2 (mid-session-surfaced horizontals emerging during analyst or architect sessions, captured in the session's orchestrator handoff and queued with an activation trigger; shaped from session context + candidate-consumer evidence when activated, no operator-authored BA-REQ expected). Names why the distinction matters — asking the operator to produce a BA-REQ for every mid-session horizontal is disproportionate. New `§7b.PROJECT` extension point for project-specific intake directory paths and filename conventions.
- **Reviewer visual-regression signal-quality ranking** (`templates/personas/reviewer.md` §2 Review Dimensions) -- new guidance for UI-heavy projects gating visual regressions. Ranks signal sources by reliability: (1) operator eyeball on screenshot pair — authoritative, slowest; (2) pixel-diff against baseline — high false-positive rate on colour/anti-aliasing shifts; (3) sentinel-SSIM on key regions — tighter than pixel-diff but catalogue-maintenance-sensitive; (4) DOM-skeleton diff — last resort, NOT reliable as arbitration (bundler artefacts like Vite `@import` merge produce large non-regression diffs). Gating rule: reviewers cannot issue PASS on DOM-skeleton diff alone; escalate to operator for visual confirmation. Flags three common anti-patterns (tests-pass-therefore-visual-OK, DOM-match-therefore-no-regression, elaborate-automation-before-signal-validation). Lifted from a real-world incident where an elaborate DOM-diff visual regression harness produced 38 "regressions" that were all Vite bundling noise.
- **Architect chain-safe task specification** (`templates/personas/architect.md` §4a) -- new section defining six mandatory conditions for tasks intended to feed an autonomous developer chain: (1) every task declares a mechanical completion signal (unit/integration test or testable CLI assertion, no UI-subjective gates), (2) test file paths named per task, (3) gates composable with the project's deterministic gate harness, (4) scope-bounded file-pattern allowlists per task, (5) per-task commit-message format declared with `[change:<slug>]` tag + task reference, (6) no latent UI dependencies in chain-bound tasks. `tasks.md` is structured as Section A (chain-safe backend) + Section B (operator-eyeball UI) with clear when-to-apply / when-NOT-to-apply guidance. Lifted to the generic architect template from a 2026-04 session-learning during real-world autonomous-chain launches, where the absence of this discipline at the architect phase propagated as noise into the dev chain. New `§4a.PROJECT` extension point for project-specific gate-harness details.
- **Orchestrator autonomous-chain pre-flight readiness check** (`templates/personas/orchestrator.md` §Autonomous loop) -- mandatory gate before any autonomous chain launch. Pre-flight verifies the chain's gate infrastructure actually executes the assertions `tasks.md` specifies, that synthetic state (fixtures, storage states, test DB seed, stub servers) is ready, and that chain halt conditions fire as designed (probe-and-revert pattern). Non-negotiable: skipping is a discipline failure. Time budget 5–15 min synchronous operator-in-loop. Halt trigger: if any pre-flight check fails, chain does NOT launch; correction prompt issued first. Lifted from a real-world incident where an autonomous chain ran 2.5h against a measurement harness that could not observe real application state — chain halted correctly but wall-clock was burned before the operator could intervene.
- **Orchestrator wall-clock discipline** (`templates/personas/orchestrator.md` §4 Generate Next Action) -- every generated prompt MUST include a wall-clock field in its Report Back section (`Wall-clock: <start ISO> → <end ISO> = <duration>`). Calibration data is lost without this. Additionally: estimates for interactive prompts (where operator must be in loop) distinguish *active interactive time* (quoted in estimates) from *elapsed wall-clock* (reported but not treated as calibration signal — it includes operator availability gaps). New calibration heuristic table covering analyst capture-mode, interactive question-review, deep-dive architecture-introduction, architect design, developer single-surface fix, developer scaffold, reviewer midpoint, reviewer boundary. Orchestrator re-calibrates against measured data from target report-backs rather than defaulting to human-time intuitions.
- **Architect commit-body minimum expectation** (`templates/personas/architect.md` §7) -- when landing multiple commits per proposal (typical pattern: design.md + tasks.md + specs/), each commit body must contain at least a one-paragraph summary distinct from the subject line. Per-commit-type body expectations defined (design.md summarises architectural approach; tasks.md summarises chain-safe count + UI-deferred count + migration-ordering; specs/ enumerates which baselines were delta'd and why). Prevents the subject-only / empty-body commits that degrade history readability even when §0.5 tag-traceability passes. Applies to any multi-commit landing, not just architect output.

- **Layered persona architecture** -- personas split into two files: base templates (`templates/personas/<role>.md`) with generic methodology and `§.PROJECT` extension points, and project overlays (`docs/AE/personas/<role>.md` in the target project) with project-specific configuration. Base templates use numbered sections (`§1`, `§2`, etc.) and named extension points (`§1.PROJECT`, `§HR.PROJECT`, `§ENV.PROJECT`). Overlays carry a Persona Header Block referencing their base template with explicit precedence rules. This separation allows methodology improvements to flow to all projects without rewriting overlays.
- **Archaeologist persona** (`templates/personas/archaeologist.md`) -- fifth engineering role that runs upstream of the Analyst → Architect → Developer → Reviewer loop. Investigates existing codebases and produces OpenSpec baseline specs (`status: baseline`) with `[verified]`/`[unverified]` tags on factual claims. Three-phase investigation methodology: structural mapping (parallel, fast), cross-referencing and judgment (parallel, capable), synthesis and verification (main context). Includes reconciliation mode for periodic re-verification.
- **Persona validation script** (`bin/validate-personas.sh`) -- checks base templates for AEH Base Template header, numbered sections, `.PROJECT` extension points, and project-specific content leakage. When given a target project path, also validates overlay files for Persona Header Block presence and base template references.
- **Autonomous loop capability** -- reviewer persona gains autonomous mode section: JSON verdict schema, deterministic gate execution before qualitative review, re-review protocol with iteration tracking and escalation to BLOCK after 3 persistent failures. Orchestrator persona gains autonomous loop mode: programmatic developer→gates→reviewer cycle, iteration tracking, escalation policy, batch execution commands, and monitor commands for reading loop state.
- **Loop driver template** (`templates/scripts/loop-driver.sh.template`) -- shell script template for the developer→gates→reviewer automation loop. Handles iteration caps, gate result parsing, reviewer verdict parsing, escalation, and state file management.
- **Orchestrator state schema extensions** -- orchestrator state template gains Active Loop State section (mode, iteration tracking, gate/reviewer results) and Escalation Policy section (iteration caps, gate failure rules, BLOCK handling, crash recovery).

### Changed
- **Architect §5 (Specification Document) re-framed as legacy fallback** -- prior content describing a flat `spec.md` with Overview / Architecture / Stack / Cross-Cutting / Implementation / Risks / Glossary sections was the pre-OpenSpec convention. §7 OpenSpec Integration is now authoritative (design.md + tasks.md + specs/ inside the change proposal directory). §5 retained as a minimal fallback for the rare pre-OpenSpec project, with explicit recommendation to adopt OpenSpec via the tools playbook before the project grows. Internal contradiction between §5 and §7 resolved.
- **README: codify the "proposal is the single instruction set" principle** -- the OpenSpec section gains an explicit articulation of the pivotal linkage that makes the AEH loop function: the analyst's `proposal.md` serves as both the architect's brief AND the reviewer's checklist, with acceptance criteria acting directly as the review checklist rather than living in a separate instructions doc. "The rigour of the proposal IS the quality of every downstream handoff." Previously implicit in persona methodology but never stated as the load-bearing principle it is.
- **Reviewer persona: enterprise-grade upgrade** -- six additions to the code reviewer base template, driven by real-world observation that reviews were being skipped and by research into agentic review best practices: (1) **Absence Check** dimension -- new dimension after Correctness that specifically checks for what's MISSING (error handling, input validation, auth, logging, resource cleanup) in new code, compensating for LLMs' structural blind spot of evaluating present code but not noticing absent code; (2) **Cross-Module Impact strengthened** from "proportional scan, non-blocking" to mandatory caller grep with broken callers as BLOCKING; (3) **Dependency Health** dimension -- verifies new imports resolve to real packages (anti-hallucination), checks for known CVEs, license compatibility, and necessity; (4) **Performance Anti-patterns** dimension -- systematised from web-only to all projects (N+1, unbounded collections, missing pagination, sync I/O in async, unnecessary materialisation); (5) **Evidence requirement** for all verdicts -- every PASS and FAIL must cite specific lines, grep results, or test output; dimensions without evidence are SKIPPED not PASS; this is the single strongest anti-rubber-stamp measure; (6) **Review Starting Point Rotation** -- reviewer varies entry point across reviews (tests, API, data layer, config, error paths) to prevent pattern-matching complacency, noted in report header.
- **Orchestrator: structural reviewer cadence enforcement** -- the orchestrator now has a hard, non-discretionary rule for reviewer scheduling: before generating any non-reviewer prompt, check `last_reviewed_task` vs `current_task` in the state file; if the gap >= 5 (Regime 1) or the task is last-in-phase (either regime), the next prompt MUST be a reviewer prompt with no exceptions. This replaces the previous implicit "generate a review when appropriate" approach that led to reviews being skipped. Additionally: (a) new Review Tracking section in the state file schema (`last_reviewed_task`, `review_cadence`, `reviews_completed`, `reviews_with_corrections`, `current_gap`); (b) phase exit prerequisite requiring reviewer PASS/WARN with corrections before sign-off; (c) review-debt proactive monitoring flag; (d) catch-up review enforcement if cadence was violated (e.g. due to context loss).
- **Orchestrator execution regimes** -- Operating Modes rewritten to define two explicit regimes: (1) Prompt-by-prompt (default, with reviewer pass every 5 tasks) and (2) Batch execution + phase-boundary review (self-chaining developer prompts per phase, reviewer batch pass at boundaries, correction prompts for HIGH findings before next phase). Both regimes include formal reviewer gates — the difference is granularity. Switchover prompt template at `templates/prompts/orchestrator-batch-regime.md`. Operator activates batch regime by saying "batch mode" or pasting the switchover template.
- **Orchestrator prompt verbosity calibration** -- new "Prompt Verbosity Calibration" subsection under §4 (Generate Next Action). Detailed prompts for phase starts, role switches, and complex prerequisites; lean prompts for mid-phase sequential tasks that reference `docs/AE/tasks.md` directly instead of the orchestrator paraphrasing the architect's spec. Prevents spec drift between orchestrator's understanding and the authoritative task file. Lean prompts retain persona loading, pre-flight, TDD, verify, commit, and report structure.
- **Orchestrator continuous improvement principle** -- new "Improve the templates, not just the memory" principle. When the orchestrator discovers a pattern that improves performance or efficiency, it must propose an update to the relevant AEH template for operator approval, not just save to local memory. Local memory is session-scoped; template improvements survive agent replacement.
- **Role-bound prompts now self-activate the role** -- orchestrator persona updated. Every handover prompt that invokes a persona begins with a "Step 0 — Activate the &lt;role&gt; role (self-contained)" block that writes `.claude/persona`, declares the session role-active, and loads the two persona files (base + overlay). The operator no longer needs to say `switch` or pick a role out of band; pasting the execute line is sufficient. Handoff protocol updated to drop the manual `switch` step and name the role inline instead. Freestyle/no-role prompts (harness setup) skip Step 0.
- **Orchestrator persona gains "Role Boundaries — Do Not Cross" section** -- explicit scope discipline preventing the orchestrator from slipping into analyst/architect/developer/reviewer work. Codifies that the orchestrator notes existence and routes target-project domain documents to the appropriate role rather than summarising, interpreting, or extracting content from them; that harness `open-questions.md` holds only harness-layer questions; and that domain/architectural content does not belong in harness artifacts. Includes a concrete "could this sentence be wrong in a way that requires domain knowledge to notice?" test and examples of content that fails/passes it. Added corresponding "Stay in manager lane" principle.
- **Onboarding playbook updated for layered personas** -- Phase 2 role mapping includes archaeologist. Phase 5 plan examples show overlay scaffolding instead of monolithic persona creation. Phase 6 §6a rewritten to describe layered persona convention: overlay header block format, `§.PROJECT` extension point population, five-role scaffolding, and merge-from-existing workflow. Phase 7 orchestrator tip mentions five-role model and two-file loading.
- **CLAUDE.md.template updated** -- role list expanded to 5 (added archaeologist), persona loading section rewritten for two-file convention (base template + project overlay with precedence rules), workflow section lists all five engineering roles.
- **Orchestrator persona updated** -- describes five-role model, layered persona loading convention with two-file instruction format in all handover prompts, Archaeologist invocation criteria (onboarding, major unspecified area, periodic reconciliation, operator request), project onboarding workflow (scaffold → boundaries → archaeologist → review → loop).
- **Harness reviewer updated** -- Dimension 4 (Template & Persona Consistency) gains layered persona checks: base template structure, extension points, no project-specific content, overlay header block validation, methodology duplication detection. References `bin/validate-personas.sh` for deterministic checking. Gains Archaeologist Baseline Specs sub-check.
- **Analyst persona updated** -- Multi-Agent Analysis section removed and replaced with Archaeologist cross-reference. Reverse-engineering mode removed (Archaeologist's responsibility). All four engineering base templates gain `§.PROJECT` extension points and base header notices.
- **Architect persona updated** -- §2 Technology Decisions marked as skippable when overlay provides `[SKIP]` with fixed constraints. Eight principles preserved including "write to workspace, not memory".
- **README.md rewritten** -- reflects five-role model, layered persona architecture, baseline specs, validation script, updated project structure tree, and current filesystem state.
- **CLAUDE.md project structure tree updated** -- adds `bin/validate-personas.sh`, `templates/personas/archaeologist.md`, `templates/scripts/`.

---

## [v0.7] - 2026-03-13 -- OpenSpec Integration & Role Maturity

OpenSpec becomes the recommended specification management layer with deep integration into all four personas. New orchestrator role manages prompt pipelines across sessions. Personas gain real-world hardening: scope escalation prevention, discovery logging, test coverage enforcement, database security checks. Sandbox environment provisioning enables MCP servers in Docker containers.

### Added
- **OpenSpec role integration** -- OpenSpec promoted from optional tool to recommended spec management layer, integrated into the four-persona workflow. Each persona template (`analyst.md`, `architect.md`, `developer.md`, `reviewer.md`) gains a "Spec Management" section handling both OpenSpec-present and OpenSpec-absent scenarios. Orchestrator gains "Spec-Aware Routing" for change-proposal-based pipeline tracking. CLAUDE.md template gains a "Specification Management" section above Key Files. Onboarding playbook Phase 6g offers OpenSpec setup with opt-out. Tools playbook presents OpenSpec as recommended-by-default. OpenSpec setup template enhanced with role integration table, example frontmatter, change proposal structure, and graceful degradation documentation. README updated to reflect OpenSpec as recommended specification management.
- **OpenSpec specification quality governance** -- governance layer now evaluates OpenSpec document quality, not just tool plumbing. Review criteria gains Section 3a (7 criteria for spec quality, conditional on `openspec/specs/` presence). Assessment checklist gains items 9.4-9.7 (frontmatter validity, staleness, abandoned proposals, testable acceptance criteria). Health-check gains Phase 3j (spec inventory, frontmatter audit, staleness detection, abandoned proposals, spec-code drift) with dedicated Spec Health table in delta reports and "Spec drift" category in terminal summary. Reviewer persona gains 3 lightweight quality checks in Spec Currency table (frontmatter, orphans, abandoned proposals -- all pass/WARN).
- **OpenSpec: no MCP server for CLI agents** -- OpenSpec setup template, teardown template, tools playbook, tools README, and CLAUDE.md.template updated to make clear that CLI agents with filesystem access (Claude Code, Aider, etc.) should NOT use the OpenSpec MCP server. Spec files are markdown readable directly; the MCP server adds a brittle intermediary for zero functional gain. MCP server setup retained only for sandboxed environments without filesystem access.
- **Orchestrator persona** (`templates/personas/orchestrator.md`) -- pipeline management role that tracks prompt execution, assesses agent output quality, maintains outcome metrics, and generates next actions. Persists state in `targets/<slug>/orchestrator-state.md` for cold-start reconstruction. Supports auto-drive and step-by-step modes with configurable quality gates. Added to valid roles in CLAUDE.md, personas table in README, workspace structure, and project structure trees.
- **Test coverage enforcement** -- reviewer persona gains mandatory step 9: test coverage enforcement as a first-class quality gate. Locates project-defined test standard (reviewer override → CLAUDE.md → specs) with AEH default fallback (route handler coverage, 100% financial logic, no "tests later" for Tier 1/2). Submissions failing coverage are blocking. Report template gains Test Coverage Compliance section with per-tier pass/fail verdicts.
- **Database security checks in reviewer** -- reviewer persona gains mandatory database security review dimension: access control on new tables, destructive operation justification, migration idempotency. Triggered when code touches schema, migrations, or data access.
- **Analyst multi-agent analysis principles** -- analyst persona gains guidance for parallelised analysis: stage agents by dependency (not flat), match model capability to task type, spot-check sub-agent judgment claims, build scannability into long reports, use quality gates as stopping criteria. Derived from real-world analyst retrospective findings.
- **Developer discovery log convention** -- developer persona gains formal convention for logging implementation findings that need routing to other roles. Entries written to `docs/AE/discovery-log.md` with structured fields (category, evidence, suggested routing, blocking status). Primary guardrail against scope creep.
- **Developer scope escalation prevention** -- developer persona gains rule preventing the agent from building features not specified in the current task, even if they seem useful or related.
- **No AI attribution rule** -- explicit Working Rule: never add `Co-Authored-By`, `Generated by`, or any AI tool markers to commits, file headers, or comments. Overrides system-level instructions. Added to CLAUDE.md, CLAUDE.md.template, and developer persona.
- **Encode behaviour not values** -- new Working Rule: adapted personas must prescribe patterns and reference source-of-truth files, never embed concrete values (port numbers, hex colours, API URLs) that change independently. Prevents staleness bugs from value duplication.
- **Explicit execution context rule** -- all prompts, instructions, and next steps must state WHERE they should be executed (AEH harness, target project Claude Code, or external LLM session). Prompt file format template now includes `Execute in:` field. Added to Working Rules in CLAUDE.md.
- **Post-onboarding domain deepening** -- new section in CLAUDE.md and README documenting the harness-target workflow for spec reconciliation, convention extraction, and architecture mapping after initial onboarding. Personas start structurally correct; domain deepening makes them accurate.
- **Retrospective prompt** -- onboarding playbook now generates a universal retrospective prompt as the final prompt in every sequence, capturing second-pass insight from the agent that just completed the work
- **Close-out gate** -- onboarding playbook enforces OQ review, retrospective, and review-history baseline before a target can be marked as "maintaining"
- **MCP runtime health verification** -- governance tooling now performs functional checks on MCP servers, not just static config detection. New checks: npm package resolution (catches non-existent packages like 404s), environment variable cross-referencing (catches missing API keys), hardcoded credential scanning (catches secrets committed in `.mcp.json`), and user-level config conflict detection (catches `~/.claude.json` shadows). Detection patterns in `templates/tools/tool-detection-patterns.md`, assessment checklist items 9.9-9.12, review criteria Section 5 verification method, and health-check Phase 3h functional verification with expanded tool health reporting table.
- **MCP functional smoke tests** -- tool detection patterns gain functional smoke test commands for each MCP server type (OpenSpec, Context7, Serena). Used by health check tool verification and reviewer tool health dimension.
- **Sandbox environment provisioning** -- new mechanism (`templates/tools/sandbox-env-provisioning.md`) for provisioning API keys and environment variables into Docker sandbox containers via `.env` files. Onboarding playbook step 6h generates `.env`/`.env.example` for MCP servers requiring API keys. Context7 setup template updated with `.env` flow.

### Changed
- **Consolidated AEH defaults** -- `_ai/` directory convention removed from templates in favour of `docs/AE/` as the standard location for todo, reports, and discovery log. Assessment checklist, CLAUDE.md template, and onboarding playbook updated.
- README workflow section rewritten as concrete two-session diagram with numbered steps
- README separates harness-internal roles (Orchestrator, Harness Reviewer) from target project personas

---

## [v0.6] - 2026-02-27 -- Governance & Permissions

Agent permission governance becomes a first-class subsystem: schema reference, detection patterns, recommended baselines, and mandatory review integration. Structural hygiene auditing catches agent-generated filesystem clutter. Harness Reviewer persona enables self-review of the harness itself.

### Added
- **Agent permission governance** -- new `templates/agents/` directory for agent-specific reference knowledge, starting with Claude Code
- `templates/agents/README.md` -- explains agents vs tools vs governance, lists known agents
- `templates/agents/claude-code/permissions.md` -- full schema reference, file precedence, rule syntax, anti-pattern catalogue (CRITICAL→LOW), remediation patterns
- `templates/agents/claude-code/permission-detection-patterns.md` -- glob/grep patterns for auditing permission configs (secrets, bypass mode, filesystem escape, sprawl, stale rules, harness isolation breach)
- `templates/agents/claude-code/permission-baselines.md` -- three recommended configs (solo/team/open-source) as embeddable JSON blocks with rationale
- Assessment checklist **Category 10: Agent Permission Governance** -- 9 items covering settings hygiene, secrets, deny lists, sprawl, filesystem scope, and file separation
- Review criteria **Rubric 6: Agent Permission Quality** -- 6 criteria with signs of good governance and common problems
- **Mandatory permission review in reviewer persona** -- every review pass must include a Permission Health section in `comments.md`, never silently skipped
- **Harness isolation check** -- CRITICAL detection pattern verifying target agent cannot read from the AEH harness directory (AP-04)
- **Review history file** (`targets/<project>/review-history.md`) -- append-only longitudinal findings log for pattern detection across assessments, always includes permission snapshot
- Health-check **Phase 3h: Permission Health Check** -- reads settings files, runs detection patterns, compares against baseline
- Health-check **permission drift** as delta report category with dedicated Permission Health section in report format
- Onboarding Phase 2b step 9: permission file detection in reconnaissance search strategy
- Onboarding Phase 2c: "Permissions" line in summary output format
- `CLAUDE.md.template`: Permission Governance section with settings file documentation, baseline reference, and maintenance rules
- **Structural hygiene audit** -- new assessment checklist items 8.9 (directory internal organisation) and 8.10 (agent-generated detritus detection). Reviewer persona gains mandatory "Structural Hygiene" review dimension (step 5) that catches filesystem clutter regardless of baseline. Health-check playbook gains step 3g: independent structural scan that applies fresh engineering judgment, not baseline comparison. Addresses the pattern where LLM agents create files prolifically and walk away.
- **Harness Reviewer persona** (`templates/personas/harness-reviewer.md`) -- dedicated self-review role for the harness itself, checking 7 dimensions: target detail leakage, prompt self-containment, documentation currency, template & persona consistency, isolation boundary integrity, governance completeness, and public-facing quality. Added to valid roles in CLAUDE.md, personas table in README, and project structure trees.
- **Target detail leakage enforcement** -- "no target details in harness files" rule added to Working Rules, covers git commit messages, references harness-reviewer as systematic enforcement mechanism
- **Git history cleaned** -- removed target-identifying details from commit messages and historical file content using git-filter-repo

### Changed
- Assessment checklist now has 10 categories (was 9)
- Review criteria now has 6 rubrics (was 5), plus "Agent permissions" row in Overall Assessment table
- Health-check remediation option 3 includes permission fixes
- Health-check Phase 5 references permission baselines for drift remediation
- Health-check phase completion appends to `review-history.md` (append-only longitudinal record)
- Onboarding Phase 3e workspace creation includes `review-history.md`
- CLAUDE.md project structure tree includes `templates/agents/` and `review-history.md`
- README project structure tree includes `templates/agents/`

### Fixed
- Removed slash prefix from natural language commands throughout templates (`/switch` → `switch`, `/health` → `health`). These are keyword triggers recognised from CLAUDE.md, not CLI slash commands.
- Orchestrator driving instructions now require role specification in every instruction
- Orchestrator role routing rule added for correct persona switching

---

## [v0.5] - 2026-02-20 -- Tool Integration & Open Source

### Added
- **AGPL-3.0 license** with `LICENSE-FAQ.md` clarifying that AEH output (personas, prompts, CLAUDE.md sections) belongs to the user and is unencumbered
- **CONTRIBUTING.md** -- prompt-first contribution model (submit the LLM prompt that produces the change, not just the diff), BDFL maintenance model, clear expectations for response times and scope
- Community infrastructure: Discord + GitLab Issues, sponsor links (GitHub Sponsors, Polar.sh)
- License badge in README
- **Post-transformation regression check** (`templates/prompts/regression-check.md.template`) -- verifies builds, import integrity, config path references, and runtime behaviour after structural transformations. Auto-generated as the final prompt in every onboarding sequence. Also triggered in health-check remediation when fix prompts move or rename files.
- `templates/prompts/` directory for reusable prompt templates
- Onboarding Phase 6d generates a regression check prompt adapted to the target project
- Health-check Phase 5 generates a regression check when remediation moves files
- **Tool integration system** for optional MCP server management (OpenSpec, Context7, Serena)
- `templates/tools/` directory with setup and teardown prompt templates for each tool
- `templates/tools/README.md` -- overview and design principles for the tool integration system
- `templates/tools/tool-detection-patterns.md` -- glob/grep patterns for detecting tools and functional equivalents
- `templates/tools/openspec-setup.md` / `openspec-teardown.md` -- OpenSpec MCP server configuration
- `templates/tools/context7-setup.md` / `context7-teardown.md` -- Context7 MCP server configuration
- `templates/tools/serena-setup.md` / `serena-teardown.md` -- Serena MCP server configuration
- `tools` playbook (`templates/playbooks/tools.md`) -- 5-phase workflow for tool detection, offering, setup/teardown, and recording
- Assessment checklist Category 8: "Project Layout & Naming Hygiene" -- directory structure, file naming, redundant/misplaced/obsolete files, documentation taxonomy
- Assessment checklist Category 9: "Development Tooling (Optional)" -- informational only, never penalises absence (renumbered from 8)
- Review criteria Rubric 5: "Tool Integration Quality (Optional)" -- scored only when tools are actively configured
- Health-check step 3g: Tool Health Check -- verifies configured tools are still present, documented, and consistent
- Health-check tool drift category in delta reports
- Onboarding Phase 2b: MCP and tool detection patterns in reconnaissance search strategy
- Onboarding Phase 6d: informational mention of `tools` after harness setup
- `CLAUDE.md.template`: optional "Development Tools" section with subsection templates for all three tools

### Fixed
- Session init now requires user confirmation before adopting a carried-over persona. Previously, a role persisted from the last session was adopted silently. Updated in harness CLAUDE.md and `CLAUDE.md.template`.

### Changed
- README expanded with Community, Supporting AEH, and License sections
- Onboarding Phase 2b detection targets table expanded with MCP, tool, and spec management rows
- Health-check Phase 4 delta report includes tool drift as a category
- Health-check Phase 5 remediation option 3 now includes tool repair
- CLAUDE.md playbooks table and role commands table include `tools`
- CLAUDE.md project structure tree updated with `templates/tools/` and `tools.md` playbook
- README updated with tool integration in features list, workflow diagram, project structure, and current status
- **Nested private repo for `targets/`** -- recommended setup for keeping private target workspaces versioned independently from the public harness repo
- CLAUDE.md documents dual-repo commit/push rules, detection, and proactive setup offering
- Onboarding Phase 3e offers nested repo setup during first workspace creation
- README documents the pattern under "Managing Target Workspace History"
- `docs/screenshots/` convention for transient human-Claude screenshot exchange (gitignored, timestamp-based)

### Removed
- `.gitlab-ci.yml` -- CI guard for target data leaks was non-functional without runners (GitLab Free tier). The `.gitignore` + nested repo structure provides sufficient protection.

---

## [v0.4] - 2026-02-17

### Added
- Strategist persona template (`templates/personas/strategist.md`) for upstream business/strategic decision support in external LLM sessions
- Maturity model in README (5 levels from assessment-only to strategic layer)
- "Who Is This For", "Quick Start", and "Current Status" sections in README
- CHANGELOG.md
- Harness Maintenance Discipline section in CLAUDE.md
- CLAUDE.md section ordering checks: assessment checklist item 2.8, review criteria "Section ordering" criterion, health-check step 3e, onboarding Phase 3b note
- Specialist prompt collection step in onboarding playbook (Phase 2d): asks users for domain-specific prompts they've been pasting manually, merges them into persona adaptations
- Domain expertise adaptation guidance in generic reviewer template, with worked example reference

### Changed
- README rewritten for public audience (cleaner structure, less internal jargon)
- Project abbreviated as AEH throughout public-facing docs
- Persona count updated from "four" to "four engineering + optional strategist" across CLAUDE.md, README, onboarding playbook
- Onboarding playbook Phase 7: light strategist mention in handoff
- `role info` output now includes strategist as optional external role
- `CLAUDE.md.template`: session init added as second section (after Project Overview), with note that section ordering matters

### Fixed
- `CLAUDE.md.template` had no session init section at all -- added it in the correct position (top of file)
- `targets/*/` now gitignored -- private project data no longer pushed to remote. Only `targets/index.md` (empty registry template) is tracked.
- Strategist template updated to two-document model: stable role definition + frequently-regenerated project knowledge briefing. Includes staleness guidance.

## [v0.3] - 2026-02-17

### Added
- Onboarding playbook (`templates/playbooks/onboarding.md`) -- 7-phase guided workflow with skip gates and re-onboarding detection
- Health-check playbook (`templates/playbooks/health-check.md`) -- recurring compliance checks with delta reports
- Assessment-implementation boundary in CLAUDE.md: onboarding never touches application code
- Merge-and-confirm rule for prompts that modify existing instruction files
- Session init and role selection (persona persistence, 3-line banner, `switch`, `role info`, `ignore role`)
- `onboard` and `health` natural language commands with playbook references

### Changed
- README updated with playbook workflow and new principles

## [v0.2] - 2026-02-15

### Added
- Two-Claude Model: harness reads and plans, target executes
- Target project isolation rule (hard boundary, one narrow exception for prompt delivery)
- `targets/` workspace structure with per-project directories
- `targets/index.md` as orientation entry point
- Prompt file format (self-contained, numbered, ordered)
- Five transformation phases: assessment, planning, implementing, reviewing, maintaining
- Direct prompt delivery policy (per-project opt-in)

### Changed
- Replaced `logs/` with `targets/` as canonical per-project state location

## [v0.1] - 2026-02-15

### Added
- Initial persona templates: Analyst, Architect, Developer, Reviewer
- Project templates: `CLAUDE.md.template`, `agents.md.template`
- Governance criteria: assessment checklist, review criteria
- Structured reference from "How I Tamed Claude" (NDC London 2026)
- CLAUDE.md with mission, working rules, and project structure
- README with problem statement, solution, and core principles
